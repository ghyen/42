#include "Zombie.hpp"

Zombie* zombieHorde( int N, std::string name)
{
    Zombie *ZombiesAddress;
    while (N)
    {
        ZombiesAddress = newZombie(<cast>string:N);
        ZombiesAddress += sizeof(Zombie);

    return (ZombiesAddress);
}
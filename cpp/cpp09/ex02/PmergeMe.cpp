#include "PmergeMe.hpp"

static void recursive(std::vector<int> input, int w)
{
    
    for (int idx = 0; idx < input.size() - w; idx++)
    {
        if (input[idx] )
    }
};

std::vector<int> &PmergeMe::solve(std::vector<int> &input)
{
    recursive(input, 2);

};
